// import type { HttpContextContract } from '@ioc:Adonis/Core/HttpContext'

// export default class ExamsController {
//   async ExamShow({view}: HttpContextContract) {
//     return view.render('exams/exam')
//   }

//   async generateExam({ request, response }: HttpContextContract) {
//     const examtype = request.input('examtype')
//     const exam_cat = request.input('exam_cat')
//     const exam_subcat = request.input('exam_subcat')
//     const number_of_questions = request.input('number_of_questions')
//     const exam_duration = request.input('exam_duration')

//     this.ExamShow()
//   }
// }
